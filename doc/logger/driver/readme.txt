LOGGER.SYS is a UPX compressed driver only. It provides the same functionality
as the driver portion of dual purpose LOGGER.COM. It just takes less disk space
and could be useful on floppy diskette when space is tight.